<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_home_search"
        android:icon="@drawable/ic_search"
        android:title="@string/activity_home_menu_search"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_home_settings"
        android:title="@string/activity_home_menu_settings"
        app:showAsAction="never"/>

    <item
        android:id="@+id/menu_home_about"
        android:title="@string/activity_home_menu_about"
        app:showAsAction="never"/>

</menu>